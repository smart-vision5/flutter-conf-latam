enum SessionLevel { basic, intermediate, advanced, expert }

enum Language { spanish, english }

enum SessionType {
  checkIn,
  keynote,
  session,
  workshop,
  breaks,
  lunch,
  coffee,
  networking,
  panel,
  finish,
}

enum SponsorTier {
  platinum,
  gold,
  silver,
  bronze,
  inKind,
  senior,
  junior,
  other,
}
